# 项目经历

## MewCode - 终端 AI 编程 Agent

**技术栈**：Go, ReAct, Function Calling, MCP, Multi-Agent, Context Engineering

**项目介绍**：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。涵盖 Agent 循环、工具系统、上下文管理、长期记忆、权限沙箱、多智能体协作等核心子系统，支持 Anthropic / OpenAI / DeepSeek 多协议接入、MCP 工具扩展、Skill 技能包、跨会话记忆，覆盖从单文件编辑到大型重构的编程场景。

**个人职责**：

- 独立完成全栈架构设计与核心模块实现，包括 Agent 循环、工具注册表、上下文管理、记忆系统、权限沙箱、多智能体协作等
- 设计并实现跨会话长期记忆系统：JSONL 会话持久化、LLM 驱动的每轮异步记忆提取、四级记忆分类与双路径存储、项目指令文件分级加载
- 负责核心技术方案选型与实现：多 LLM 协议统一适配、两层渐进式上下文压缩、MCP 工具延迟加载、Skill 技能体系、多 Agent 协作框架

**技术亮点**：

- **MCP 工具延迟加载**：MCP 工具注册时仅将名称注入 system-reminder，完整 JSON Schema 按需通过 ToolSearch 发现后加载。在 58 个 MCP 工具的会话建模下，工具描述 Token 占用减少 80% 以上，避免上下文窗口被工具定义挤占，同时保持模型对可用工具的感知
- **多 LLM 协议统一适配**：抽象 StreamEvent 统一事件接口（TextDelta / ThinkingDelta / ToolCallStart / ToolCallComplete / StreamEnd 等），将 Anthropic Messages API、OpenAI Responses API、OpenAI 兼容 Chat Completions API 三套异构流式协议归一为同一事件流，新增供应商只需实现 Client 接口
- **两层渐进式上下文压缩**：
  - Layer 1 工具结果裁剪 + 预算控制，基于 ContentReplacementState 冻结已见 tool_use_id 的替换决策，保证模型可见前缀字节稳定，命中 Anthropic Prompt Cache
  - Layer 2 在 Token 用量达上下文窗口 80% 时触发 LLM 摘要，保留近期消息原文仅对历史前缀摘要；摘要请求超窗口时按 API 轮次分组丢弃最老分组重试（最多 3 次），避免压缩本身因 prompt_too_long 失败
  - 压缩边界持久化到会话文件，resume 时重建压缩态而非重放全量；自动对齐 Function Calling 的 tool_use ↔ tool_result 调用配对，避免孤立 tool_result 触发协议错误，支持数小时连续编程会话不丢失上下文
- **多级权限拦截模型**：覆盖 Plan 模式例外、安全只读命令白名单、危险命令正则检测（rm -rf /、mkfs、fork bomb、curl|sh、git 破坏性命令等）、路径沙箱（受保护配置文件不可写）、YAML 声明式规则引擎、会话级 allow-always、权限模式矩阵、HITL 人工确认。复合命令（&&/||/;/|）拆分后逐条校验，防止 `cmd1 && dangerous_cmd` 绕过检查；配合 macOS seatbelt / Linux bubblewrap OS 级沙箱，支持 Agent 全自动模式安全执行
- **每轮自动记忆提取**：挂载于 Agent 循环 OnLoopComplete 回调，每轮对话结束后异步 fork 子 Agent 回顾新消息，将四类记忆（用户偏好 / 纠正反馈 / 项目知识 / 参考信息）分类持久化到用户级或项目级目录。带并发合并（in-flight 合并 trailing run）、互斥去重（主 Agent 已写记忆则跳过 fork）、cursor 推进、优雅 Drain 关闭。跨会话知识持续累积，同类纠正一次后自动规避，新会话自动继承项目上下文无需重复交代
- **项目指令分级加载**：按优先级加载用户级（~/.mewcode/MEWCODE.md、AGENTS.md）→ 项目级（git root 到 workDir 逐层）→ local 私有覆盖，支持 @include 指令递归引用外部文件，循环安全、跳过代码块
- **多 Agent 协作**：基于 Git Worktree 实现文件级隔离避免编辑冲突，支持 in-process / tmux / iTerm 三种后端，成员间通过文件邮箱异步通信。Coordinator 模式下 Lead Agent 工具白名单仅保留调度与只读工具（剔除 WriteFile / EditFile），只负责任务拆分与结果汇总不参与编码，突破单 Agent 上下文窗口瓶颈
